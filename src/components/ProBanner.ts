/**
 * ProBanner — disabled on CYBERSPACE (everything is free).
 * All exports are kept as stubs so existing callers don't break.
 */

export function showProBanner(_container: HTMLElement): void {
  // No-op: CYBERSPACE has no upsell banners
}

export function hideProBanner(): void {
  // No-op
}

export function isProBannerVisible(): boolean {
  return false;
}
