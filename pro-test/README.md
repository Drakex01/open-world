# CYBERSPACE Pro Test Suite

This directory contains standalone tests and verification utilities for CYBERSPACE.

## Run Locally

**Prerequisites:** Node.js

1. Install dependencies:
   `npm install`
2. Run the development server:
   `npm run dev`
